fork {
  loop("LOOP0") { while (eval("true")) {
    if (isActive("update", "LOOP0")) return
  } } // LOOP0
} forkAgain {
  loop("LOOP1") { while (eval("true")) {
    if (isActive("deactivate", "LOOP1")) return
    if (isActive("activate", "LOOP1")) return
  } } // LOOP1
}; if (endFork()) return
