if (isActive("update")) return
fork("FORK1") {
  loop("LOOP2") { while (eval("true")) {
    if (isActive("update", "LOOP2")) return
  } } // LOOP2
} forkAgain("FORK1") {
  loop("LOOP3") { while (eval("true")) {
    if (isActive("deactivate", "LOOP3")) return
    if (isActive("activate", "LOOP3")) return
  } } // LOOP3
}; if (endFork("FORK1")) return
