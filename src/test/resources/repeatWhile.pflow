loop("LOOP1") { do { loopBlock("LOOP_BLOCK2") {
  if (isActive("read data", "LOOP_BLOCK2")) return
  if (isActive("generate diagrams", "LOOP_BLOCK2")) return
} } while (eval("more data?", "yes", "LOOP1")) } // not ("no")
