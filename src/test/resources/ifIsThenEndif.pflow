rootBlock("ROOT_BLOCK0") {
  conditional("CONDITIONAL1") { if (eval("new String('yes')", "yes", "CONDITIONAL1")) { ifBlock("IF_BLOCK2") { // is
    isActive("process all", "IF_BLOCK2")
  } } } // CONDITIONAL1
}
