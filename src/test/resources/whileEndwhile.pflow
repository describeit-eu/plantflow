rootBlock("ROOT_BLOCK0") {
  loop("LOOP1") { while (eval("check filesize ?", "not empty", "LOOP1")) { loopBlock("LOOP_BLOCK2") { // is
    isActive("read file", "LOOP_BLOCK2")
  } } } // empty LOOP1
  isActive("close file", "ROOT_BLOCK0")
}
