loop("LOOP1") { while (eval("check filesize ?", "not empty", "LOOP1")) { loopBlock("LOOP_BLOCK2") { // is
  if (isActive("read file", "LOOP_BLOCK2")) return
} } } // empty LOOP1
if (isActive("close file", "SEQ0")) return
