conditional("CONDITIONAL1") { if (eval("actions['process all']")) { // yes
  if (isActive("process all", "CONDITIONAL1")) return
} else { // no CONDITIONAL1
  if (isActive("process none", "CONDITIONAL1")) return
} } // CONDITIONAL1
