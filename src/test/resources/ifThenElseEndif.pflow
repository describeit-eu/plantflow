conditional("CONDITIONAL0") { if (eval("actions['process all']")) { // yes
  if (isActive("process all", "CONDITIONAL0")) return
} else { // no CONDITIONAL0
  if (isActive("process none", "CONDITIONAL0")) return
} } // CONDITIONAL0
