/*
@startuml
while (check filesize ?) is (not empty)
  :read file;
endwhile (empty)
:close file;
@enduml
*/

start()
whilee ('check filesize ?') is ('not empty')
  action('read file')
endwhile ('empty')
action('close file')
end()
