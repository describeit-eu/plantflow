# Record Value Tokens

Tokens are modeled as immutable record values containing identity, timestamp, and payload attributes. Immutability ensures deterministic state transitions, prevents accidental side effects during guard evaluations, and facilitates step-by-step history replay and state verification.
